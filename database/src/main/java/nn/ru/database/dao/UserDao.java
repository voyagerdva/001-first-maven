package nn.ru.database.dao;

import nn.ru.database.entity.User;

import java.util.Optional;

public class UserDao {
    public Optional<User> findById(Long id) {
        return Optional.empty();
    }
}
